import 'package:sonr_app/theme/theme.dart';
import 'nav_controller.dart';

class SonrBottomSheet extends GetView<NavController> {
  @override
  Widget build(BuildContext context) {
    return Stack(children: []);
  }
}
