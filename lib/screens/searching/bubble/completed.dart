part of 'bubble.dart';

class CompletedBubble extends StatelessWidget {
  const CompletedBubble({ Key key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: const Color(0xFF2DBD3A));
  }
}